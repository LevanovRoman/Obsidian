### 🟩 **1. Основы Java (Core Java)**

- Что такое JDK, JRE, JVM и в чем их различия?
    
- Какие примитивные типы данных есть в Java?
    
- Что такое автобоксинг и анбоксинг?
    
- Разница между `==` и `.equals()`
    
- Что такое `final`, `static`, `this`, `super`?
    
- Области видимости переменных (scope)
    
- Как работает сборка мусора (Garbage Collector)?
    
- Разница между Stack и Heap
    
- Что такое immutability (например, `String`)?
    
- Различие между `String`, `StringBuilder`, `StringBuffer`
    
- Что такое перегрузка (overloading) и переопределение (overriding) методов?
    
- Порядок инициализации классов и блоков
    

---

### 🟨 **2. Объектно-Ориентированное Программирование (ООП)**

- Принципы ООП: Наследование, Инкапсуляция, Полиморфизм, Абстракция
    
- Разница между `interface` и `abstract class`
    
- Что такое SOLID-принципы?
    
- Принцип работы композиции и наследования
    

---

### 🟦 **3. Коллекции Java (Java Collections Framework)**

- Отличие `ArrayList` и `LinkedList`
    
- Разница между `HashMap`, `TreeMap`, `LinkedHashMap`
    
- Как работает `HashMap` внутри? (особенно hashCode/equals)
    
- Что такое `Set`, и как он отличается от `List`
    
- Как выбрать подходящую коллекцию под задачу
    
- Сортировка коллекций (`Comparable`, `Comparator`)
    

---

### 🟧 **4. Исключения (Exceptions)**

- Разница между `checked` и `unchecked` исключениями
    
- Как создается собственное исключение
    
- Как работает `try-catch-finally`
    
- Что произойдет, если в `finally` есть `return`?
    

---

### 🟫 **5. Работа с файлами и ввод/вывод (I/O)**

- Как читать/писать файлы в Java
    
- Разница между `InputStream` / `Reader`
    
- Что такое `BufferedReader`, `Scanner` и когда что использовать
    

---

### 🟥 **6. Многопоточность (Concurrency, basic level)**

- Что такое поток (Thread)?
    
- Разница между `Runnable` и `Thread`
    
- Ключевое слово `synchronized`
    
- Что такое `volatile`
    
- Простейшие примеры с `wait`, `notify`
    

---

### 🟦 **7. Java 8 и новее (если требуется)**

- Что такое лямбда-выражения
    
- Основы Stream API: `map`, `filter`, `collect`
    
- Что такое `Optional`
    
- Метод `forEach`
    
- Интерфейсы `Function`, `Predicate`, `Consumer`
    

---

### 🟨 **8. Spring (часто спрашивают даже на junior)**

- Что такое Spring?
    
- Основы Spring Core: @Component, @Autowired
    
- Что такое Inversion of Control и Dependency Injection
    
- Контекст приложения Spring
    
- Что такое Spring Boot и зачем он нужен
    
- Простое CRUD-приложение на Spring Boot
    

---

### 🟩 **9. Работа с БД (SQL + JDBC + JPA/Hibernate)**

- Базовые SQL-запросы: `SELECT`, `JOIN`, `INSERT`, `UPDATE`, `DELETE`
    
- Что такое первичный ключ, внешний ключ, индексы
    
- Разница между `INNER JOIN`, `LEFT JOIN`
    
- Как подключиться к БД через JDBC
    
- Что такое ORM, как работает JPA/Hibernate
    
- Основы аннотаций: `@Entity`, `@Id`, `@Column`, `@OneToMany` и т.д.
    

---

### 🟦 **10. Алгоритмы и структуры данных (на базовом уровне)**

- Сортировка пузырьком, выбором, вставками
    
- Поиск в массиве и списке
    
- Работа с рекурсией
    
- Стек, очередь, список, дерево (базово)
    

---

### 🟨 **11. Инструменты и окружение**

- Что такое Maven/Gradle
    
- Как собрать проект
    
- Что такое GIT, основные команды (`clone`, `commit`, `push`, `pull`, `branch`)
    
- Работа с IntelliJ IDEA